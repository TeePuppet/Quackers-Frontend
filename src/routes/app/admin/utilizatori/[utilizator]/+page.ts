// import { getUser } from "$lib/stores/users";


/** @type {import('./$types').PageLoad} */
export async function load(params) {
    // const userData =  await getUser(params.params.utilizator)
    // return { userData }
}